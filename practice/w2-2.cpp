#include <bits/stdc++.h>
using namespace std;
vector <int> nums;
int main(void)
{
    int n,m;
    cin >> n >> m;
    for (int i = 0 ; i < n; i++)
    {
        int input;
        cin >> input;
        nums.push_back(input);
    }
}